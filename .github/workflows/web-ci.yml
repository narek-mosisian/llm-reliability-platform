name: Web CI

on:
  pull_request:
    paths:
      - "apps/web/**"
      - ".github/workflows/web-ci.yml"
  push:
    branches:
      - main
    paths:
      - "apps/web/**"
      - ".github/workflows/web-ci.yml"

permissions:
  contents: read

jobs:
  web-ci:
    name: Web CI
    runs-on: ubuntu-latest
    timeout-minutes: 10

    defaults:
      run:
        working-directory: apps/web

    env:
      NEXT_TELEMETRY_DISABLED: "1"
      COREPACK_ENABLE_DOWNLOAD_PROMPT: "0" # disables interactive Corepack prompts

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm # enables dependency caching for pnpm so that subsequent CI runs are faster
          cache-dependency-path: apps/web/pnpm-lock.yaml
          # Tell GitHub which lockfile is relevant for the cache.
          # If this file changes, the cache is updated or renewed.

      - name: Set up pnpm
        run: corepack enable && corepack prepare pnpm@10.32.1 --activate

      - name: Install web dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ESLint
        run: pnpm lint

      - name: Build web app
        run: pnpm build
